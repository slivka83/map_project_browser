# План работ по отзывам 4 ревьюеров

Источник: `docs/feedback_2.md` … `docs/feedback_5.md`. Каждый пункт сверен с текущим кодом — помечено что уже сделано, что устарело, что стоит делать.

---

## Группа 1 — Отклонено (уже сделано или не нужно)

| № | Предложение | Верdict | Почему |
|---|---|---|---|
| 1.1 | `ErrorBoundary` обернуть `GlobeScene`/`Map2D` (fb2, fb5) | **уже есть** | `src/components/ErrorBoundary.tsx` существует; в `App.tsx:55` обёрнут `GlobeScene`; есть юнит-тест `ErrorBoundary.test.tsx` |
| 1.2 | Береговые линии через `LineSegments` вместо отдельных `<Line>` (fb2, fb4) | **уже есть** | `Globe.tsx:78-86` — один `Float32Array` буфер, один draw call |
| 1.3 | `quadSelfIntersects` отсутствует → код не компилируется (fb2) | **устарело** | Определена в `projectionMapper.ts:331` (рецензент читал обрезанный файл) |
| 1.4 | `fitProjectionToView` не экспортируется (fb2) | **устарело** | Экспортирована в `projectionMapper.ts:505` |
| 1.5 | `setParam: any` → строгая типизация (fb3) | **уже есть** | `useAppStore.ts:164` — `<K extends keyof ProjectionParams>(key: K, value: ProjectionParams[K])` |
| 1.6 | A11y: `aria-label`, `aria-pressed`, `role` (fb3, fb5) | **уже есть** | Все кнопки/тоглы/слайдеры имеют `aria-*`; `Toggle` → `role="switch"`, `CircularSlider` → `role="slider"`, модалки → `role="dialog"` |
| 1.7 | Убрать `isEllipsoid` из spec (fb3) | **устарело** | Файл `docs/specification.md` никогда не существовал; `isEllipsoid` удалён давно; AGENTS.md — источник правды |
| 1.8 | `DistortionSelect` обёртка над `Dropdown` (fb2) | **устарело** | Компонента нет; `distortion` ставится неявно через вариант проекции |
| 1.9 | Убрать тогл `showBorders`, всегда показывать границы (fb2) | **отклонено** | Это пользовательский偏好; тогл оставляем |
| 1.10 | Убрать `detailedMap`, всегда макс. детализация (fb2) | **отклонено** | 50m vs 110m — реальный performance-трейд-офф, тогл нужен |
| 1.11 | Не грузить одновременно 110m и 50m (fb2) | **отклонено** | 110m → 3D глобус, 50m → 2D карта; разные цели |
| 1.12 | Убрать `falseEasting`/`falseNorthing` из UI (fb2) | **уже есть** | В UI их и нет; только в EPSG-пресетах |
| 1.13 | Tissot через `InstancedMesh` (fb4) | **отклонено** | Индикатрисы рисуются в 2D SVG (`Map2D.tsx:278-280`), не в 3D |
| 1.14 | Синхронизация камер 3D↔2D (fb4) | **отклонено** | Архитектурно разделено осознанно: 3D-сцена визуализирует *установку* проекции (источник света, поверхность), а 2D-карта — *результат* проекции |
| 1.15 | Перенести проект из `/mnt/d` в нативный WSL (fb5) | **отклонено** | Противоречит AGENTS.md; выбран persistent symlink-подход, не требующий setup после ребута |
| 1.16 | Web Workers для D3 (fb3, fb4) | **отклонено** | Избыточно для 110m/50m; текущая мемоизация достаточна |
| 1.17 | Custom GPU-шейдеры для 3D→2D морфа (fb4) | **отклонено** | Крупная архитектурная переработка, вне scope pet-проекта |
| 1.18 | Тепловая карта искажений на 2D-карте (fb2) | **уже есть** | `HeatmapOverlay` в `Map2D.tsx:36-65` |
| 1.19 | Поперечные/косые проекции (fb2) | **уже есть** | `transverseMercator`, `obliqueMercator` варианты |
| 1.20 | Snapshot-тесты SVG, E2E с Playwright (fb2) | **отклонено** | AGENTS.md §9.3: WebGL/`<Canvas>` и SVG `d` намеренно не тестируются |
| 1.21 | LightSource через `PointLight` + raycasting (fb4) | **отклонено** | Уже реализовано математически в `auxSurfaceGeometry.ts` (`computeCentralMeridianRays`, `azLight` modes) |
| 1.22 | Tissot: расчёт полуосей a/b через Якобиан (fb4) | **отклонено** | Текущий подход (`d3Geo.geoCircle().radius(5)`) даёт тот же визуальный результат — окружности на сфере естественно искажаются проекцией в эллипсы |

---

## Группа 2 — Уже частично сделано (можно улучшить)

| № | Тема | Что есть | Что предлагается доделать |
|---|---|---|---|
| 2.1 | Обработка ошибок `loadGeoData` (fb2, fb5) | `geoLoading` флаг + `_geoToken` (защита от stale fetch) + per-file try/catch + оверлей «Загрузка геоданных…»/«Нет геоданных» в `Map2D.tsx:251-255` | Добавить `geoDataError: string \| null` в стор + кнопку «Повторить» в оверлей |
| 2.2 | `applyPreset` сброс distortion (fb2) | Если `preset.variant` не соответствует `preset.family`, variant сбрасывается к дефолту вместе с distortion | Рассмотреть edge-case: preset = `{family}` без variant, текущий variant уже этой family → distortion не сбрасывается |
| 2.3 | Conic `phiOrigin < 10` magic jump (fb3) | `signedStandardParallelDeg` с fallback на 30° — осознанный костыль (конус вырожден у экватора) | Опционально: блокировка конуса при `\|phiOrigin\| < 10` или авто-сдвиг на 15° |

---

## Группа 3 — К обсуждению (реальные кандидаты на реализацию)

| № | Предложение | Источник | Приоритет |
|---|---|---|---|
| 3.1 | `geoDataError` state + кнопка «Повторить» в `loadGeoData` | fb2, fb5 | высокий |
| 3.2 | Тест на ошибку загрузки геоданных | fb2, fb5 | высокий |
| 3.3 | Coverage: `@vitest/coverage-v8` + thresholds + CI step | fb5 | средний |
| 3.4 | Debounce пересчёта проекции при движении слайдеров | fb3 | средний |
| 3.5 | Min/max метки на слайдерах в `ParamSlider` | fb2 | низкий |
| 3.6 | `Dropdown` `'' as T` → `null` для `allLabel` (type safety) | fb2 | низкий |
| 3.7 | Рефакторинг: вынести `useElementSize` в `src/hooks/` | fb3 | низкий |
| 3.8 | Рефакторинг: разбить `ControlPanel.tsx` (515 строк) | fb3 | низкий |
| 3.9 | Рефакторинг: селектор `useCurrentMapData` | fb5 | низкий |
| 3.10 | JSDoc на ключевые функции (`computeAuxSurfaceParams`, и т.д.) | fb2, fb3 | низкий |
| 3.11 | Уменьшить плотность точек гратулы в 3D (3°→5°) | fb2 | низкий |

---

## Группа 4 — Опциональные nice-to-have

| № | Предложение | Источник |
|---|---|---|
| 4.1 | Горячие клавиши (`R` — сброс, `T` — Tissot) | fb2 |
| 4.2 | Анимация перехода при смене проекции | fb2 |
| 4.3 | Сравнение проекций бок о бок | fb2 |
| 4.4 | Storybook для UI-компонентов | fb5 |
| 4.5 | Codecov badge в README | fb5 |
| 4.6 | Документация: «как добавить новую проекцию» | fb2 |

---

## Принятые решения

(Заполняется по ходу обсуждения — каждое решение с обоснованием и статусом: ✅ принято / ❌ отклонено / ⏸ отложено)

| № | Пункт | Решение | Статус |
|---|---|---|---|
| 3.1 | Обработка ошибок `loadGeoData` (дружелюбное сообщение вместо «Нет геоданных») | ✅ Реализовано: добавлено поле `geoDataError` в стор, оверлей `Map2D` показывает «Не удалось загрузить карту. Проверьте подключение к интернету и перезагрузите страницу.» когда оба базовых слоя (110m+50m land) не загрузились. Кнопку «Повторить» НЕ добавляли — пользователь перезагружает страницу. Тесты: расширен `useAppStore.test.ts` (3 теста: partial failure → null, total failure → friendly message, recovery clears error). AGENTS.md обновлён. | ✅ выполнено |
| 3.2 | Тест на ошибку загрузки геоданных | ✅ Выполнено в рамках 3.1 — добавлены 3 теста в `useAppStore.test.ts` | ✅ выполнено |
| 3.3 | Измерение покрытия кода тестами | ✅ Реализовано локально: добавлен `@vitest/coverage-v8@^4.1.10`, команда `npm run test:coverage`, секция `coverage` в `vitest.config.ts` (V8 provider, reporters text/html/lcov, исключения для 3D-сцены и style-токенов per §9.3). Без порогов, без CI, без Codecov. Текущее покрытие: 90.56% stmts / 80.54% branches / 87.16% funcs / 92.23% lines. `coverage/` добавлен в `.gitignore` и в `eslint.config.js` ignores. AGENTS.md + README обновлены. | ✅ выполнено |
| 3.4 | Debounce пересчёта проекции при движении слайдеров | ❌ Отклонено: текущая мемоизация (`useMemo` для проекции/пути) уже соответствует AGENTS.md «real-time perf requirement». Мгновенное обновление карты при движении ползунка — это учебная ценность (видно как проекция морфится); задержка убрала бы этот эффект. Реальных тормозов не зафиксировано. | ❌ отклонено |
| 3.5 | Min/max метки на слайдерах | ❌ Отклонено: дорожка ползунка визуально уже показывает границы; добавление текста добавит шум. Для неочевидных параметров диапазон можно увидеть в подсказке (`title`). | ❌ отклонено |
| 3.6 | `Dropdown` `'' as T` → `null` | ❌ Отклонено: `''` — это сознательный sentinel «показать все»/«нет значения» в API `Dropdown`. Тест на строке 37-43 явно проверяет `onChange('')`. Все вызывающие места уже корректно конвертируют `''` → `null`. Переделка на `null` потребует менять API, все вызовы и тест ради косметики. | ❌ отклонено |
| 3.7 | Вынести `useElementSize` в `src/hooks/` | ✅ Реализовано: хук перенесён в `src/hooks/useElementSize.ts`, из `Map2D.tsx` удалено инлайн-определение и неиспользуемые импорты `useLayoutEffect`/`useRef`/`useState`. Тесты/линт/сборка зелёные. | ✅ выполнено |
| 3.8 | Разбить `ControlPanel.tsx` (515 строк) | ✅ Частичный разбор: универсальные `ParamSlider` и `PresetChips` вынесены в `src/components/ui/ParamSlider.tsx` и `src/components/ui/PresetChips.tsx`. Доменно-специфичные `StdParallel2Control`, `ProjectionParamsSection`, `VisualizationSection` оставлены инлайн (тесно связаны с `variantDef`). Файл уменьшен с 515 до ~435 строк. AGENTS.md + README обновлены. | ✅ выполнено |
| 3.9 | Селектор `useCurrentMapData` | ❌ Отклонено: 4 строки логики, один потребитель (`Map2D`), поведение не меняется. Польза от селектора минимальна. | ❌ отклонено |
| 3.10 | JSDoc на ключевые функции | ❌ Отклонено: над каждой ключевой функцией уже есть развёрнутые `//`-комментарии (5-15 строк с объяснением математики и edge-cases). Код уже задокументирован, просто не в JSDoc-формате. | ❌ отклонено |
| 3.11 | Уменьшить плотность точек гратулы в 3D (3°→5°) | ❌ Отклонено: 6000 точек (~72KB) не нагрузка для современного GPU; draw calls зависят от числа линий (~50), не от точек; 3° даёт более плавные кривые. | ❌ отклонено |
| 2.2 | `applyPreset` сброс distortion (edge-case) | ❌ Отклонено: текущая логика в `useAppStore.ts:319` уже корректно сбрасывает distortion при несовместимости variant/family. Все реальные вызовы (`EpsgCatalog`) передают family+variant+distortion вместе. Теоретическая проблема не возникает на практике. | ❌ отклонено |
| 2.3 | «Магический прыжок» конуса у экватора | ❌ Отклонено: fallback на 30° при |phiOrigin| < 10° математически необходим (конус вырожден у экватора). Прыжок у границы 10° приемлем для учебного инструмента. | ❌ отклонено |
| 4.1 | Горячие клавиши (R, T, B, H) | ❌ Отклонено пользователем | ❌ отклонено |
| 4.2 | Анимация перехода при смене проекции | ❌ Отклонено пользователем (крупная работа) | ❌ отклонено |
| 4.3 | Сравнение проекций бок о бок | ❌ Отклонено пользователем (крупная архитектурная работа) | ❌ отклонено |
| 4.4 | Storybook для UI-компонентов | ❌ Отклонено пользователем (инфраструктура избыточна для проекта с ~6 UI-компонентами, unit-тесты уже есть) | ❌ отклонено |
| 4.5 | Codecov badge в README | ❌ Отклонено ранее в 3.3 (локальный отчёт без Codecov) | ❌ отклонено |
| 4.6 | Документация «как добавить новую проекцию» | ❌ Отклонено пользователем | ❌ отклонено |

### Мелкие пункты (после основного обзора)

| № | Пункт | Решение | Статус |
|---|---|---|---|
| М.А | `worldPerPixel` в `constants/geometry.ts` (fb2) | ❌ Уже там (`geometry.ts:16`) — fb2 устарел | ❌ устарело |
| М.Б | `EpsgCatalog` 6 `useState` → один объект (fb2) | ❌ Отклонено: текущие 6 `useState` ясные, апдейты однострочные. Объединение сделает их многословнее без пользы. | ❌ отклонено |
| М.В | Тип `GeoPoint = [number, number]` для координат (fb4) | ❌ Отклонено: косметика. Код уже последовательно использует `[number, number]`, конвенция понятна из контекста. | ❌ отклонено |
| М.Г | `RING_SEGMENTS` 96 → 64 (fb2) | ❌ Отклонено: микро-оптимизация (96 уже лёгкая для GPU); менять константу в 3D-коде (исключён из тестов per §9.3) без визуальной проверки рискованно; визуальной разницы нет. | ❌ отклонено |

---

## Итог обзора

| Категория | Кол-во | Принято | Отклонено |
|---|---|---|---|
| Группа 1 (уже сделано/устарело) | 22 | — | 22 |
| Группа 2 (частично сделано) | 3 | 1 (через 3.1) | 2 |
| Группа 3 (к обсуждению) | 11 | 5 | 6 |
| Группа 4 (nice-to-have) | 6 | 0 | 6 |
| Мелкие пункты | 4 | 0 | 4 |
| **Всего** | **46** | **6** | **40** |

**Принятые и выполненные изменения (закрыты коммитами):**
1. **3.1 + 3.2** — Дружелюбное сообщение об ошибке загрузки геоданных вместо сухого «Нет геоданных» + 3 регрессионных теста
2. **3.3** — Локальный отчёт покрытия кода через `@vitest/coverage-v8` (90.56% stmts / 80.54% branches)
3. **3.7** — Хук `useElementSize` вынесен в `src/hooks/`
4. **3.8** — `ParamSlider` и `PresetChips` вынесены в `src/components/ui/`

Все принятые изменения сопровождались: код + тесты + обновление AGENTS.md/README + lint/test/build зелёные + коммит.
