// Projection-related interfaces (specification.md §2: src/types).
// Re-exported from the store so there is a single source of truth.
export type {
  ProjectionFamily,
  DistortionModel,
  ProjectionParams,
} from '../store/useAppStore';
